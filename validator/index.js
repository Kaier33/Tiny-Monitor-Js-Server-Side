const authSchema = require('./auth');
const generalSchema = require('./general');
const userScheme = require('./user');
const projectsSchema = require('./projects');

module.exports = { authSchema, generalSchema, userScheme, projectsSchema };
